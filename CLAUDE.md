@AGENTS.md

Claude Code specifics: the Unity MCP server is declared in `.mcp.json` and is approved automatically by `.claude/settings.json`. Use `/route` for any feature that touches more than one file.
